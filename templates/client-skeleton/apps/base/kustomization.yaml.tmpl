apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# App workloads (Deployments, Services, Ingresses). Add per-app resources
# under apps/base/<app>/ and reference them here.
resources: []
