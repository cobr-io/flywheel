apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Per-app builder folders live under builders/base/<app>/, scaffolded by
# `flywheel add app <name>`. The CLI appends each new app to the list
# below; you can also edit by hand.
resources: []
